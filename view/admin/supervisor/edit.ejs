<div class="main-panel">
    <div class="content-wrapper">
        <div class="card col-12">
            <div class="card-body">
                <div class="titleSec">
                    <!-- <button type="button" class="btn btn-primary" id="myBtn">Add</button> -->
                    <h2><i class="fa fa-arrow-circle-left" aria-hidden="true" id="back"
                            style="cursor: pointer;">&nbsp;Edit Agent Details</i></h2>
                </div>
                <form action="/admin/supervisor/edit" id="myForm" method="post">
                    <input type="hidden" name="ardb_id" value="<%= req_data.ardb_id %>">
                    <div class="titleSec">
                        <h5>Branch : <%= data.branch_name %>
                        </h5>
                    </div>
                    <div class="row">

                        <div class="form-group col-6">
                            <label for="ardb_id"><b>ARDB Name:</b></label>
                            <select name="ardb_id" class="form-control" id="ardb_id" disabled>
                                <option value="">Select ARDB Name</option>
                                <% if(ardbList.length > 0){
                                    ardbList.forEach(dt => {
                                        var _selected = (req_data.ardb_id == dt.ardb_id) ? 'selected' : ''; %>
                                        <option value="<%= dt.ardb_id %>" <%= _selected %>>
                                            <%= dt.ardb_name %>
                                        </option>
                                    <% })
                                } %>
                            </select>
                        </div>
                        <div class="form-group col-6">
                            <label for="branch_c">Branch Code:</label>
                            <input type="text" id="branch_c" name="branch_c" class="form-control"
                                value="<%= data.branch_code %>" <%= agent_id > 0 ? 'readonly' : '' %> />
                        </div>
                        <div class="form-group col-6">
                            <label for="supervisor_code">Supervisor Code:</label>
                            <input type="text" id="supervisor_code" name="user_id" class="form-control"
                                value="<%= data.supervisor_code %>" <%= agent_id> 0 ? 'readonly' : '' %> />
                        </div>

                        <div class="form-group col-6">
                            <label for="supervisor_name">Name:</label>
                            <input id="supervisor_name" type="text" name="supervisor_name" class="form-control" placeholder="Enter Name"
                                value="<%= data.supervisor_name %>" />
                        </div>

                        <div class="form-group col-6">
                            <label for="email">Email:</label>
                            <input type="email" id="email" name="email" class="form-control"
                                value="<%= data.email_id %>" <%= agent_id> 0 ? 'readonly' : '' %> />
                        </div>

                        <div class="form-group col-6">
                            <label for="mobile">Mobile No.:</label>
                            <input type="text" id="mobile" name="mobile" class="form-control"
                                value="<%= data.phone_no %>" />
                        </div>

                        <div class="form-group col-6">
                            <label for="max_amt">MAX Allowable Collection Amount :</label>
                            <input id="max_amt" type="number" name="max_amt" class="form-control"
                                value="<%= data.max_amt %>" />
                        </div>
                        <div class="form-group col-6">
                            <label for="allow_collection_days">Max allowable no.of days :</label>
                            <input type="number" name="allow_collection_days" class="form-control"
                                id="allow_collection_days" value="<%= data.allow_collection_days %>" />
                        </div>

                        <div class="form-group col-6">
                            <label for="deviceId">Device Id. :</label>
                            <input id="deviceId" type="text" name="device_id" class="form-control"
                                value="<%= data.device_id %>" />
                        </div>
                        <div class="form-group col-6">
                            <label for="printer_type">Printer Type :</label>
                            <select name="printer_type" id="printer_type" class="form-control" required>
                                <option value="">Select Printer Type</option>
                                <% if(Object.keys(printer_type).length> 0){
                                    Object.keys(printer_type).forEach(dt => {
                                    var _selected = (data.printer_type == dt) ? 'selected' : ''; %>
                                    <option value="<%= dt %>" <%=_selected %>>
                                        <%= printer_type[dt] %>
                                    </option>
                                    <% }) } %>
                            </select>
                        </div>
                        <div class="form-group col-6">
                            <label for="print_opt">Printer Type :</label>
                            <select name="print_opt" id="print_opt" class="form-control" required>
                                <option value="">Select Printer Type</option>
                                <% if(Object.keys(print_opt).length> 0){
                                    Object.keys(print_opt).forEach(dt => {
                                    var _selected = (data.print_opt == dt) ? 'selected' : ''; %>
                                    <option value="<%= dt %>" <%=_selected %>>
                                        <%= print_opt[dt] %>
                                    </option>
                                    <% }) } %>
                            </select>
                        </div>

                        <div class="form-group col-6 mt-4">
                            <label for="success"></label>
                            <input type="checkbox" value="Y" id="myCheckbox" data-toggle="toggle" data-onstyle="success"
                                data-offstyle="danger" data-on="Active" data-off="Deactive" name="agent_active"
                                <%=(data.active_flag=='Y' )?'checked':''%>
                            >

                        </div>

                        <input type="hidden" name="agent_id" value="<%= agent_id %>">

                        <div class="form-group col-6">
                            <div class="col-6">
                                <input type="submit" value="SUBMIT" class="btn btn-primary btn-block" />
                            </div>

                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function () {
        $("#back").click(function () {
            event.preventDefault();
            history.back(1);
        });
    });
</script>